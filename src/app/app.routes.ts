import { Routes } from '@angular/router';
import { UserProfileComponent } from './selectors/user-profile/user-profile.component';

export const routes: Routes = [
  { path: 'sin-selector', component: UserProfileComponent}
];
